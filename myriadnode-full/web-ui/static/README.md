# Static Assets

Place static assets in this directory:

- `favicon.png` - Site favicon (recommended 32x32 or 64x64 PNG)
- Other static files as needed

These files will be copied to the build output and served from the root path.
